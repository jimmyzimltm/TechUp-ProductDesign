<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Design Game Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/css/style.css" >

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-M0NXM8FB79"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', 'G-M0NXM8FB79');
    </script>

</head>

<body>
    <main class="container">
        <!-- Navbar which can be reused across other pages -->
        <%- include('../partials/navbar'); %>

        <!-- Welcome information -->
        <div class="topline-container">
            <h1>🏆Leaderboard🏆</h1>
            <p>See all the top scores so far</p>

        </div>
        <table>
            <TR>
                <TD class="emphasis"> UserName </TD>
                <TD class="emphasis"> <span class="emoji">📦</span>Total Score </TD>
                <TD> <span class="emoji">🔎</span>Problem Statement Clarity </TD>
                <TD> <span class="emoji">💭</span>Customer Insight </TD>
                <TD> <span class="emoji">💖</span>Team Morale </TD>
                <TD> <span class="emoji">💪</span>Business Owner Confidence</TD>
            </TR>
        <!-- Show top scores -->
        <% savedscores.forEach(post => { %>
          
                <tr>
                    <td class="emphasis"><%= post.username %></td>
                    <td class="emphasis"><%= post.score %></td>
                    <td><%= post.psc %></td>
                    <td><%= post.insight %></td>
                    <td><%= post.morale %></td>
                    <td><%= post.boc %></td>
                    <!-- <td>                
                        <form action="/delete/<%= post.id %>" method="post" onsubmit="return confirm('Do you really want to delete this score?');">
                        <button class="outline primary" type="submit">Delete</button>
                        </form>
                    </td> -->
                </tr>
        <% }); %>
        </table>
    </main>
</body>
</html>